[Unit]
Description=Reviewer cadence — hourly, 24/7 (registry mutations only). Cut from 3x/hr (Jun 18) to curb MiniMax token burn.

[Timer]
OnCalendar=*-*-* *:00:00 UTC
Persistent=false
Unit=judas-reviewer.service

[Install]
WantedBy=timers.target
